import { UserProfileDetailsDTO } from './user-profile-details-dto.model';

describe('UserProfileDetailsDTO', () => {
  it('should create an instance', () => {
    expect(new UserProfileDetailsDTO()).toBeTruthy();
  });
});
